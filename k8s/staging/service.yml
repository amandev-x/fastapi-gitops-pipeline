kind: Service 
apiVersion: v1 
metadata:
  name: fastapi-app
  namespace: staging
  labels:
    app: fastapi-app
spec:
  type: NodePort 
  selector:
    app: fastapi-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30081
      protocol: TCP